# A persona is a hidden writing VOICE the agent writes in (never shown — the byline is the
# author). Personas are shareable across authors; the corpus is every post written in this voice.
id: default
name: "House voice"
style:
  guide: "Conversational, concise, technical when needed."
  # references: ["https://example.com/glossary"]
